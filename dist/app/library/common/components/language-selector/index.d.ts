import { PropsWithChildren } from "react";
interface LanguageSelectorProps {
    lang: string;
}
declare const LanguageSelector: (props: PropsWithChildren<LanguageSelectorProps>) => JSX.Element;
export default LanguageSelector;
